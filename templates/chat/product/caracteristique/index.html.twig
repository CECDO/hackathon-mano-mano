{% extends 'base.html.twig' %}

{% block title %}Hello CaracteristiqueController!{% endblock %}

{% block body %}


<main class="mh-100 mw-100" >
    
    <section class="chatbot col-3 mh-100 shadow position-fixed bottom-0 end-0 m-2 mb-5" style="border-radius: 0.5rem;">
        <div class="card">
            <div class="bg-primary p-3 hover-overlay ripple shadow-2-strong p-3 rounded-top" data-mdb-ripple-color="light">
              <a class="d-flex " href="#">
                  <img class="w-25" src="https://i.ibb.co/TwcK46V/Mano-Manoicon.png" alt="ManoBot">
                  <h1 class="text-light h3 text-center d-flex align-items-center pl-2" style="padding-left:20px;">Bienvenue sur ManoBot</h1>
                
              </a>
            </div>
            <div class="card-body p-5">
                <div class="position-relative">
                    <img src="https://i.ibb.co/9vhGnVD/chaboticon.png" alt="" class="position-absolute top-0 start-0 translate-middle p-1" style="max-width:6vh;">
              <a class="btn btn-outline-primary" style="border-radius: 0.5rem;">
                Veuillez selectionner la categorie de votre produit</a>
                </div>
                <div class="d-flex flex-column my-2 col-8">
                {% for category in categories %}
              <a href="{{path('chat_category_id', {'id': category.id} )}}" class="btn my-2 " style="border-radius: 0.5rem; width:15vh; height:15vh;">
                  <i class="fas fas-8x">{{product.category.icon}} </i>
                  <p class="d-flex justify-content-center">{{category.alt}}</p>
                </a>
                {% endfor %}
            </div>

            <div class="position-relative">
                <a href="#" class="btn btn-primary my-1 position-absolute end-0" style="border-radius: 0.5rem;">Suivre une commande</a>
                <img src="https://i.ibb.co/4ZZwtT1/avatr-chatbot.png" alt="" class="p-1 position-absolute top-0 start-100 translate-middle p-1" style="max-width:5vh;">
                
            </div>
            </div>

            <p>Veuillez saisir le type de produit dans la barre de recherche</p>
    {% if product.name == "Lit" %}
        {{ product.name }}
    {% endif %}

{% for product in caracteristique.products %}
        {{ product.name }}
{% endfor %}

<p>Veuillez selectionner la caracteristique:</p>
    

    {% if product.name == "Lit" %}
        <p>KingSize</p>
    {% endif %}

    {% if product.name == "Perceuse" %}
        {{ caracteristique.name }}
    {% endif %}

            {% if caracteristique.name == "Sans-Fil" %}
        <p>Quelle type de materiaux voulez vous percer ?</p>
        <button> <a href="/materiaux/1">dur (ex: beton)</a></button>
        <button><a href="">souple (ex: placo)</a></button>
    {% endif %}

    {% if caracteristique.name == "KingSize" %}
        <p>Quelle type de dimension voulez vous ?</p>
        <button>200x200</button>
        <button>180x200</button>
    {% endif %}

            <div class="form-outline m-3" >
                <input type="text" id="typeText" class="form-control"/>
                <label class="form-label" for="typeText">Saisissez votre requête</label>
            </div>
          </div>
 
    </section>

            {% endblock %}
